# Subject
Write a function that displays the character passed as a parameter. The function should be prototyped as follows:
```
void ft_putchar(char c);
```
To display the character, you must use the **write** function as follows:
```
write(1, &c, 1);
```